runtime: python38

handlers:
- url: /.*
  script: main.app

env_variables:
  CLOUD_SQL_USERNAME: postgres
  CLOUD_SQL_DATABASE_NAME: postgres
  CLOUD_SQL_CONNECTION_NAME: ranked-choice-voting-383118:us-central1:ranked-choice
  DISCORD_REDIRECT_URI: "https://ranked-choice-voting-383118.uc.r.appspot.com/callback"
